#include <iostream>
#include <vector>
#include <unordered_map>
#include <algorithm>

using namespace std;



int find_sol(vector<string>& holder){
    
}


int main(){
    int r;
    int c;
    string row;
    cin>>r;
    cin>>c;

    vector<string> holder(r);

    while (r--)
    {
        cin.ignore();
        getline(cin, row);
        holder.push_back(row);

    }


    
}